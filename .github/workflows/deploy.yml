name: 🚀 Deploy SSG

on:
  push:
    branches: [main]
    paths:
      - 'data/**'
      - 'public/admin/**'
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🟢 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'  # ← Изменено с 'npm' на 'pnpm'

      - name: 📦 Install pnpm
        uses: pnpm/action-setup@v4  # ← Добавлена установка pnpm
        with:
          version: 9
          run_install: false

      - name: 📦 Install dependencies
        run: pnpm install --frozen-lockfile  # ← pnpm вместо npm ci

      - name: 🔨 Generate static site
        run: pnpm run generate  # ← pnpm вместо npm
        env:
          NUXT_PUBLIC_SITE_URL: ${{ secrets.NUXT_PUBLIC_SITE_URL }}

      - name: 📤 Deploy to Yandex Object Storage
        uses: shallwefootball/s3-upload-action@master
        with:
          aws_key_id: ${{ secrets.YC_ACCESS_KEY }}
          aws_secret_access_key: ${{ secrets.YC_SECRET_KEY }}
          aws_bucket: ${{ secrets.YC_BUCKET_NAME }}
          aws_region: ru-central1
          source_dir: '.output/public'
          destination_dir: '/'
          delete_removed_files: true

      - name: ✅ Deploy complete
        run: echo "Сайт успешно обновлён!"