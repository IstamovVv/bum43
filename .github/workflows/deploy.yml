name: 🚀 Deploy to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'data/**'
      - 'public/admin/**'
      - 'pages/**'
      - 'components/**'
      - 'nuxt.config.ts'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Install pnpm
        run: npm install -g pnpm@9

      - name: 📦 Install dependencies
        run: pnpm install --frozen-lockfile

      - name: 🔨 Generate static site
        run: pnpm run generate
        env:
          NUXT_PUBLIC_SITE_URL: https://istamovvv.github.io/bum43

      - name: 📤 Setup Pages
        uses: actions/configure-pages@v4

      - name: 📤 Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./.output/public  # ← Самое важное: путь к собранным файлам!

      - name: 🚀 Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4